name: release
on:
  push:
    branches: [master]

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release:
    uses: jaetill/agentic-dev-environment/.github/workflows/release.yml@main
    with:
      release-type: "node"
    secrets: inherit

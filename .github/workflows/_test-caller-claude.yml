name: _test-caller-claude
# THROWAWAY PROBE — delete after the #443 caveat test (ADR-0018 work).
# Calls the platform's _test-reusable-claude.yml cross-repo to verify
# claude-code-action's App-auth works inside a uses: reusable.
on:
  workflow_dispatch:
permissions:
  contents: read
  id-token: write
jobs:
  call:
    uses: jaetill/agentic-dev-environment/.github/workflows/_test-reusable-claude.yml@main
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
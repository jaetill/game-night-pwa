## Dependency Watch (2026-06-15)

> Generated by dep-watcher · manifests scanned: 3 (`package.json`, `lambda/package.json`, `mcp/package.json`)

---

### `package.json` (root / frontend)

**Audit (prod deps):** 0 vulnerabilities · 1 production dependency (Vite + dev tooling omitted via `--omit=dev`)

**Outdated:** none

No actionable findings.

---

### `lambda/package.json`

**Audit (prod deps):** 0 vulnerabilities

**Outdated:** 3 packages have a newer major version available

> Note: `lambda/node_modules/` is not installed in CI. Results are based on `package-lock.json` locked versions vs npm registry latest.

#### Major version bumps (breaking-change risk — review before upgrading)

| Package | Locked | Latest | Range in package.json |
|---|---|---|---|
| `@octokit/rest` | 21.1.1 | **22.0.1** | `^21.0.0` |
| `@sentry/aws-serverless` | 9.47.1 | **10.58.0** | `^9.0.0` |
| `aws-jwt-verify` | 4.0.1 | **5.2.1** | `^4.0.1` |

**Upgrade notes:**
- **`@octokit/rest` 21 → 22**: Check Octokit v22 changelog for REST client method changes. Used only in `lambda/feedback.js` for filing GitHub issues — test `issues.create()` call signature.
- **`@sentry/aws-serverless` 9 → 10**: Sentry SDK major version; `wrapHandler` API may have changed. All 8 Lambda handlers use `Sentry.wrapHandler` — verify wrapper signature and DSN config remain compatible.
- **`aws-jwt-verify` 4 → 5**: Used in `lambda/apiKeyAuthorizer.js` for Cognito JWT verification. Critical auth path — validate JWKS fetch and `CognitoJwtVerifier` API before deploying.

#### Minor/patch (batch in monthly sweep)

None — all AWS SDK packages (`@aws-sdk/client-*`, `@aws-sdk/s3-request-presigner`) are current at 3.1068.0.

---

### `mcp/package.json`

**Audit (prod deps):** 6 vulnerabilities — **2 HIGH, 4 MODERATE** — all in transitive dependencies of `@modelcontextprotocol/sdk`. All have `fixAvailable: true`.

> Note: `mcp/node_modules/` is not installed in CI. Results are based on `mcp/package-lock.json` locked versions.

#### HIGH severity — flag for immediate update

| Package | Locked | Advisory | CVSS | Details |
|---|---|---|---|---|
| `fast-uri` | 3.1.0 | [GHSA-q3j6-qgpj-74h6](https://github.com/advisories/GHSA-q3j6-qgpj-74h6) | 7.5 | Path traversal via percent-encoded dot segments (≤3.1.0) |
| `fast-uri` | 3.1.0 | [GHSA-v39h-62p7-jpjc](https://github.com/advisories/GHSA-v39h-62p7-jpjc) | 7.5 | Host confusion via percent-encoded authority delimiters (≤3.1.1) |
| `fast-xml-builder` | 1.1.5 | [GHSA-5wm8-gmm8-39j9](https://github.com/advisories/GHSA-5wm8-gmm8-39j9) | 6.1 | XML injection — attribute values with unwanted quotes bypass attribute sanitization (≤1.1.6) |

#### MODERATE severity

| Package | Locked | Advisory | CVSS | Details |
|---|---|---|---|---|
| `fast-xml-builder` | 1.1.5 | [GHSA-45c6-75p6-83cc](https://github.com/advisories/GHSA-45c6-75p6-83cc) | 6.1 | Comment value regex bypass (=1.1.5) |
| `hono` | 4.12.15 | [GHSA-qp7p-654g-cw7p](https://github.com/advisories/GHSA-qp7p-654g-cw7p) + 7 more | 3.8–6.5 | Multiple: CSS injection, JWT validation, cache leakage, bodyLimit bypass, JSX HTML injection, IPv6 IP restriction bypass, Set-Cookie injection, JWT auth scheme (all <4.12.21) |
| `ip-address` | 10.1.0 | [GHSA-v2v4-37r5-5v8g](https://github.com/advisories/GHSA-v2v4-37r5-5v8g) | — | XSS in `Address6` HTML-emitting methods (≤10.1.0); pulled in via `express-rate-limit` |
| `qs` | 6.15.0 | [GHSA-q8mj-m7cp-5q26](https://github.com/advisories/GHSA-q8mj-m7cp-5q26) | 5.3 | DoS via `qs.stringify()` crash on null/undefined entries in comma-format arrays (6.11.1–6.15.1) |

**Remediation:** Run `cd mcp && npm audit fix` — all 6 advisories report `fixAvailable: true`. Re-test MCP server after fix (tool calls use `@modelcontextprotocol/sdk` routing and `@aws-sdk/client-s3` presigned URL generation).

#### Outdated (direct dependencies)

Both direct `mcp/` dependencies are current: `@modelcontextprotocol/sdk` 1.29.0 and `@aws-sdk/client-s3` 3.1068.0 are at their npm `latest`.

---

### Summary

| Manifest | Critical | High | Moderate | Major bumps | Status |
|---|---|---|---|---|---|
| `package.json` (root) | 0 | 0 | 0 | 0 | ✅ Clean |
| `lambda/package.json` | 0 | 0 | 0 | 3 | ⚠️ Major bumps — review before upgrading |
| `mcp/package.json` | 0 | 2 | 4 | 0 | 🔴 Run `npm audit fix` in `mcp/` |

**Immediate actions:**
1. `cd mcp && npm audit fix` — resolves 2 HIGH + 4 MODERATE advisories in transitive deps
2. Plan lambda major-version upgrades for `aws-jwt-verify` (auth critical), `@sentry/aws-serverless`, and `@octokit/rest` with changelog review and testing before each

## Dependency Watch (2026-06-08)

> Generated by dep-watcher. Covers all `package.json` manifests (root, `lambda/`, `mcp/`).
> `npm audit` run with `--omit=dev`; `npm outdated` compared against latest registry tag.

---

## `package.json` (root — frontend / Vite build tooling)

### Security advisories
None.

### Outdated packages
None. All production dependencies are current.

---

## `lambda/package.json`

### Security advisories
None.

### Outdated packages

#### Major version bumps — breaking-change risk (act before next Lambda deploy)

| Package | Installed range | Latest in range (wanted) | Latest available | Notes |
|---|---|---|---|---|
| `@octokit/rest` | `^21.0.0` | 21.1.1 | **22.0.1** | Major bump; review changelog for API surface changes in `feedback.js` |
| `@sentry/aws-serverless` | `^9.0.0` | 9.47.1 | **10.56.0** | Major bump; Sentry v10 changes SDK init and `wrapHandler` API |
| `aws-jwt-verify` | `^4.0.1` | 4.0.1 | **5.2.1** | Major bump; used in `apiKeyAuthorizer.js`; verify JWKS fetch and claim-assertion interfaces |

#### Minor / patch bumps — low priority (batch in monthly sweep)

| Package | Installed range | Latest in range |
|---|---|---|
| `@aws-sdk/client-cognito-identity-provider` | `^3.633.0` | 3.1063.0 |
| `@aws-sdk/client-s3` | `^3.633.0` | 3.1063.0 |
| `@aws-sdk/client-secrets-manager` | `^3.633.0` | 3.1063.0 |
| `@aws-sdk/client-ssm` | `^3.633.0` | 3.1063.0 |
| `@aws-sdk/s3-request-presigner` | `^3.633.0` | 3.1063.0 |

All five AWS SDK packages track together — a single `npm install @aws-sdk/...@latest` sweep in `lambda/` covers all of them.

---

## `mcp/package.json`

### Security advisories — **ACTION REQUIRED**

#### HIGH severity (2 packages, 3 CVEs)

| Package | Advisory | CVSS | CVE description | Fix |
|---|---|---|---|---|
| `fast-uri` | [GHSA-q3j6-qgpj-74h6](https://github.com/advisories/GHSA-q3j6-qgpj-74h6) | 7.5 | Path traversal via percent-encoded dot segments | `npm audit fix --prefix mcp` |
| `fast-uri` | [GHSA-v39h-62p7-jpjc](https://github.com/advisories/GHSA-v39h-62p7-jpjc) | 7.5 | Host confusion via percent-encoded authority delimiters | same |
| `fast-xml-builder` | [GHSA-5wm8-gmm8-39j9](https://github.com/advisories/GHSA-5wm8-gmm8-39j9) | 6.1 | Attribute-value quote bypass allowing XML injection (CWE-91, CWE-611) | `npm audit fix --prefix mcp` |

Both packages are transitive dependencies of `@modelcontextprotocol/sdk`. Fix is available via `npm audit fix`.

> **Remediation:** run `npm audit fix --prefix mcp` to pull patched transitive versions. Verify with `npm audit --prefix mcp --omit=dev` afterward. If `--audit-fix` bumps the `@modelcontextprotocol/sdk` lockfile entry, commit `mcp/package-lock.json`.

#### MODERATE severity (4 packages / advisory chains)

| Package | Advisory | CVSS | Summary | Fix available |
|---|---|---|---|---|
| `hono` | [GHSA-qp7p-654g-cw7p](https://github.com/advisories/GHSA-qp7p-654g-cw7p) | 4.3 | CSS declaration injection via JSX SSR style objects | Yes |
| `hono` | [GHSA-p77w-8qqv-26rm](https://github.com/advisories/GHSA-p77w-8qqv-26rm) | 5.3 | Cache middleware ignores `Vary: Authorization / Cookie` → cross-user cache leakage | Yes |
| `hono` | [GHSA-9vqf-7f2p-gf9v](https://github.com/advisories/GHSA-9vqf-7f2p-gf9v) | 6.5 | `bodyLimit()` bypass for chunked/unknown-length requests (CWE-400) | Yes |
| `hono` | [GHSA-69xw-7hcm-h432](https://github.com/advisories/GHSA-69xw-7hcm-h432) | 4.7 | Unvalidated JSX tag names → HTML injection | Yes |
| `hono` | [GHSA-xrhx-7g5j-rcj5](https://github.com/advisories/GHSA-xrhx-7g5j-rcj5) | 5.3 | IP restriction bypass for non-canonical IPv6 | Yes |
| `hono` | [GHSA-3hrh-pfw6-9m5x](https://github.com/advisories/GHSA-3hrh-pfw6-9m5x) | 4.3 | Cookie helper doesn't sanitize `sameSite`/`priority` → Set-Cookie injection | Yes |
| `hono` | [GHSA-f577-qrjj-4474](https://github.com/advisories/GHSA-f577-qrjj-4474) | 4.8 | JWT middleware accepts any `Authorization` scheme, not only Bearer | Yes |
| `ip-address` | [GHSA-v2v4-37r5-5v8g](https://github.com/advisories/GHSA-v2v4-37r5-5v8g) | — | XSS in `Address6` HTML-emitting methods; surfaces via `express-rate-limit` | Yes |
| `express-rate-limit` | (via `ip-address`) | — | Inherits XSS risk from `ip-address ≤10.1.0` | Yes |
| `qs` | [GHSA-q8mj-m7cp-5q26](https://github.com/advisories/GHSA-q8mj-m7cp-5q26) | 5.3 | DoS: `qs.stringify` crashes on null/undefined in comma-format arrays with `encodeValuesOnly` | Yes |

All moderate findings are covered by the same `npm audit fix --prefix mcp` run.

### Outdated packages — low priority (batch in monthly sweep)

| Package | Installed range | Latest in range (wanted) |
|---|---|---|
| `@aws-sdk/client-s3` | `^3.700.0` | 3.1063.0 |
| `@modelcontextprotocol/sdk` | `^1.0.0` | 1.29.0 |

---

## Summary

| Manifest | CRITICAL | HIGH | MODERATE | Major bumps | Minor/patch |
|---|---|---|---|---|---|
| `package.json` (root) | 0 | 0 | 0 | 0 | 0 |
| `lambda/package.json` | 0 | 0 | 0 | 3 | 5 |
| `mcp/package.json` | 0 | 2 pkg / 3 CVE | 4 pkg / 10 CVE | 0 | 2 |

### Immediate actions
1. **`mcp/`** — `npm audit fix --prefix mcp` to resolve HIGH `fast-uri` and `fast-xml-builder` advisories (and all moderate findings in one pass). Commit the updated `mcp/package-lock.json`.

### Planned actions
2. **`lambda/`** — Evaluate and upgrade `@octokit/rest` → 22.x, `@sentry/aws-serverless` → 10.x, and `aws-jwt-verify` → 5.x individually (each is a major bump with potential API changes).
3. **`lambda/`** — Batch-update the five `@aws-sdk` v3 minor bumps (`^3.633.0` → `^3.1063.0`) in a single lockfile update.
4. **`mcp/`** — Update `@modelcontextprotocol/sdk` to `^1.29.0` and `@aws-sdk/client-s3` to `^3.1063.0` in the same batch sweep as the Lambda AWS SDK update.

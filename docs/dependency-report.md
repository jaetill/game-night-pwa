## Dependency Watch (2026-07-13)

---

## `package.json` (root)

### Security Advisories

None. `npm audit` found **0 vulnerabilities** across 595 dependencies (593 dev, 1 prod, 1 optional-transitive-counted).

### Outdated Packages

None. All packages are current.

---

## `lambda/package.json`

### Security Advisories

None. `npm audit --omit=dev` found **0 vulnerabilities** across 89 prod dependencies.

### Outdated Packages

None. All packages resolve to their latest published versions:

| Package | Spec | Latest |
|---|---|---|
| `@aws-sdk/client-cognito-identity-provider` | `^3` | 3.1085.0 |
| `@aws-sdk/client-dynamodb` | `^3` | 3.1085.0 |
| `@aws-sdk/client-s3` | `^3` | 3.1085.0 |
| `@aws-sdk/client-secrets-manager` | `^3` | 3.1085.0 |
| `@aws-sdk/client-ssm` | `^3` | 3.1085.0 |
| `@aws-sdk/s3-request-presigner` | `^3` | 3.1085.0 |
| `@octokit/rest` | `^22` | 22.0.1 |
| `@sentry/aws-serverless` | `^10` | 10.65.0 |
| `aws-jwt-verify` | `^5` | 5.2.1 |

---

## `mcp/package.json`

> **Note:** `mcp/` has no committed lockfile and no installed `node_modules`. The audit below reflects npm's resolved dependency tree for `@modelcontextprotocol/sdk ^1.0.0` (resolves to 1.29.0).

### Security Advisories — HIGH (flag for immediate update)

#### `fast-uri` ≤3.1.1 (transitive via `@modelcontextprotocol/sdk`)

Two HIGH-severity advisories in the same package, both with CVSS 7.5:

| Advisory | Title | CVE/GHSA |
|---|---|---|
| GHSA-q3j6-qgpj-74h6 | Path traversal via percent-encoded dot segments | CWE-22 |
| GHSA-v39h-62p7-jpjc | Host confusion via percent-encoded authority delimiters | CWE-436 |

Fix available: `npm audit fix --prefix mcp`

#### `hono` ≤4.12.24 (transitive via `@modelcontextprotocol/sdk`)

One HIGH + multiple MODERATE advisories in `hono`. The high-severity finding:

| Advisory | Title | CVSS | GHSA |
|---|---|---|---|
| GHSA-88fw-hqm2-52qc | CORS middleware reflects any `Origin` with credentials when `origin` defaults to wildcard | 7.1 (HIGH) | CWE-942 |

Additional moderate findings in `hono` (same package, all fixed by upgrading to ≥4.12.25):

| Advisory | Title | CVSS | GHSA |
|---|---|---|---|
| GHSA-xrhx-7g5j-rcj5 | IP restriction bypasses static deny rules for non-canonical IPv6 | 5.3 | CWE-185 |
| GHSA-3hrh-pfw6-9m5x | Cookie helper does not sanitize `sameSite`/`priority` — Set-Cookie injection | 4.3 | CWE-113 |
| GHSA-f577-qrjj-4474 | JWT middleware accepts any Authorization scheme, not only Bearer | 4.8 | CWE-285 |
| GHSA-2gcr-mfcq-wcc3 | `app.mount()` strips mount prefix using undecoded path | 5.3 | CWE-444 |
| GHSA-wwfh-h76j-fc44 | Path traversal in `serve-static` on Windows via encoded backslash | 5.9 | CWE-22 |
| GHSA-j6c9-x7qj-28xf | Lambda adapter merges multiple `Set-Cookie` headers, dropping cookies on ALB | 5.3 | CWE-116 |
| GHSA-rv63-4mwf-qqc2 | Body limit middleware bypassed on AWS Lambda via understated `Content-Length` | 6.5 | CWE-345 |
| GHSA-wgpf-jwqj-8h8p | Lambda@Edge adapter drops repeated request headers | 4.8 | CWE-348 |
| GHSA-qp7p-654g-cw7p | CSS declaration injection via style object values in JSX SSR | 4.3 | CWE-74 |
| GHSA-69xw-7hcm-h432 | Unvalidated JSX tag names — HTML injection | 4.7 | CWE-74 |
| GHSA-9vqf-7f2p-gf9v | `bodyLimit()` bypassed for chunked/unknown-length requests | 6.5 | CWE-400 |
| GHSA-p77w-8qqv-26rm | Cache middleware ignores `Vary: Authorization`/`Vary: Cookie` — cross-user cache leakage | 5.3 | CWE-524 |
| GHSA-hm8q-7f3q-5f36 | Improper validation of `NumericDate` claims (`exp`/`nbf`/`iat`) in JWT verify() | 3.8 | CWE-1284 |

Fix available: `npm audit fix --prefix mcp`

### Security Advisories — MODERATE

#### `ip-address` ≤10.1.0 (transitive, also via `express-rate-limit`)

| Advisory | Title | GHSA |
|---|---|---|
| GHSA-v2v4-37r5-5v8g | XSS in `Address6` HTML-emitting methods | CWE-79 |

#### `express-rate-limit` 8.0.1–8.5.0 (transitive)

Affected via `ip-address` (above). Fix available.

#### `qs` 6.11.1–6.15.1 (transitive)

| Advisory | Title | CVSS | GHSA |
|---|---|---|---|
| GHSA-q8mj-m7cp-5q26 | `qs.stringify` crashes (DoS) on `null`/`undefined` entries in comma-format arrays when `encodeValuesOnly` is set | 5.3 | CWE-476 |

Fix available: `npm audit fix --prefix mcp`

### Outdated Packages

None. Direct dependencies resolve to their latest published versions:

| Package | Spec | Resolves to |
|---|---|---|
| `@modelcontextprotocol/sdk` | `^1.0.0` | 1.29.0 |
| `@aws-sdk/client-s3` | `^3.1085.0` | 3.1085.0 |

### Recommended remediation

The mcp/ directory has no lockfile. Generating one and running `npm audit fix` will pull updated transitive dependencies:

```sh
cd mcp
npm install          # generates package-lock.json
npm audit fix        # upgrades transitive deps to fixed ranges
```

Commit the resulting `package-lock.json` to prevent uncontrolled resolution in future audits.

---

*Report generated by dep-watcher. All findings are production-dependency scoped (`--omit=dev`).*
